# Required: Admin API key for /admin/* and /web/* endpoints
# Generate with: openssl rand -base64 32
ADMIN_API_KEY=your-secure-api-key-here

# Required: Secret key appended to registration data before hashing
# WARNING: Changing this after deployment invalidates all existing registrations!
# Client software must use the same secret for offline validation.
REGISTRATION_SECRET=your-registration-secret-here

# Optional: Database file path overrides config.yaml (default: ./registrations.db)
# DB_PATH=./registrations.db

