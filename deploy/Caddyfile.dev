# ============================================
# Local development
# ============================================
localhost:8443 {
    tls internal
    encode gzip

    reverse_proxy localhost:8080

    log {
        output stdout
        format console
    }
}
